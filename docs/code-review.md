---
applyTo: '**/*.ts,**/*.tsx'
---

# Use function declaration with named export

# Destructure props with proper rest handling

# Move constants outside the component

# Use useCallback/useMemo for stable references

# Anything pass to child as prop need useCallback/useMemo for stable references

# Keep JSX shallow, break into subcomponents if needed

# Use semantic HTML elements

# Add JSDoc documentation
